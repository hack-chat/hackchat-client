/**
 * Exports a styled div element, this wraps the message portion
 * of a normal chat message event
 */

import styled from 'styled-components';

const CaptchaStyle = styled.div`
  font-size: 5px;
  white-space: pre;
`;

export default CaptchaStyle;
