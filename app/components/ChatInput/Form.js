/**
 * Exports a styled `reactstrap` Form
 * - https://reactstrap.github.io/components/form/
 */

import styled from 'styled-components';
import { Form } from 'reactstrap';

export default styled(Form)`
  background-color: rgba(0 0 0 0%);
  margin-bottom: 20px;
`;
