/**
 * Exports an extended MessageContent component
 */
import styled from 'styled-components';
import MessageContent from './MessageContent';

export default styled(MessageContent)`
  @media (width >= 768px) {
    /* im sure we will need this at some point */
  }
`;
