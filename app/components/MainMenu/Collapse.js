/**
 * Exports a styled `reactstrap` Collapse
 * - https://reactstrap.github.io/components/collapse/
 */

import styled from 'styled-components';
import { Collapse } from 'reactstrap';

export default styled(Collapse)`
  max-width: 70px;
`;
