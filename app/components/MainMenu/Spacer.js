/**
 * Exports a styled div element that provides a margin of space between
 * the bottom of the page and the main menu button
 */

import styled, { css } from 'styled-components';

const style = css`
  height: 56px;
`;

export default styled.div`
  ${style}
`;
